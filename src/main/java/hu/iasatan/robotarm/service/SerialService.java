package hu.iasatan.robotarm.service;

import org.springframework.stereotype.Service;

@Service
public class SerialService {

    /*
    private void PutZerosBeforeNumberIfNeceserry(byte number, SerialPort port) {
        if (number < 100) {
            if (number < 10)
                port.Write("0");
            port.Write("0");
        }
    }

     */
}
